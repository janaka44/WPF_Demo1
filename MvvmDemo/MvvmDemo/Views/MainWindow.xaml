<Window x:Class="MvvmDemo.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="300" Width="300">
    <Grid>
    <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
        <Label>Customer Name:</Label>
        <TextBox Width="136" Text="{Binding Customer.Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True }"/>
            <!-- <ComboBox IsEditable="True" Width="136" Text="{Binding Customer.Name, UpdateSourceTrigger=PropertyChanged}"/>-->
        <Button Command="{Binding UpdateCommand}">Update</Button>
    </StackPanel>
    </Grid>
</Window>
